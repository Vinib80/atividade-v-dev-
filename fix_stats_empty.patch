--- a/frontend/src/pages/StatsPage.tsx
+++ b/frontend/src/pages/StatsPage.tsx
@@ -82,3 +82,6 @@
             <div style={{ display: 'flex', flexDirection: 'column', gap: 14 }}>
-              {topRevenue.map((p, i) => (
+              {topRevenue.length === 0 ? (
+                <p style={{ color: 'var(--text3)', fontSize: '0.85rem', textAlign: 'center', padding: '16px 0' }}>Sem dados suficientes para ranking</p>
+              ) : topRevenue.map((p, i) => (
                 <div key={p.id_produto} style={{ cursor: 'pointer' }} onClick={() => navigate(`/product/${p.id_produto}`)}>
@@ -103,3 +106,6 @@
             <div style={{ display: 'flex', flexDirection: 'column', gap: 14 }}>
-              {topSales.map((p, i) => (
+              {topSales.length === 0 ? (
+                <p style={{ color: 'var(--text3)', fontSize: '0.85rem', textAlign: 'center', padding: '16px 0' }}>Sem dados suficientes para ranking</p>
+              ) : topSales.map((p, i) => (
                 <div key={p.id_produto} style={{ cursor: 'pointer' }} onClick={() => navigate(`/product/${p.id_produto}`)}>
@@ -126,3 +132,6 @@
             <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
-              {topRated.map((p, i) => (
+              {topRated.length === 0 ? (
+                <p style={{ color: 'var(--text3)', fontSize: '0.85rem', textAlign: 'center', padding: '16px 0' }}>Sem dados suficientes para ranking</p>
+              ) : topRated.map((p, i) => (
                 <div key={p.id_produto} style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', cursor: 'pointer' }}
